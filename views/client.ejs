<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Client - <%= client.name %></title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="page-wrapper">
        <div class="sidebar">
            <div class="sidebar-header">
                <a href="/admin/dashboard" class="logo">SMTP Panel</a>
            </div>
            <nav class="sidebar-nav">
                <a href="/admin/dashboard">Dashboard</a>
                <a href="/admin">Clients</a>
                <a href="/admin/settings">Settings</a>
                <a href="/logout">Logout</a>
            </nav>
        </div>
        <main class="main-content">
            <div class="container">
                <div class="header">
                    <h1>Manage Client: <%= client.name %></h1>
                    <a href="/admin">&larr; Back to All Clients</a>
                </div>
                <p><strong>API Key:</strong> <code><%= client.apiKey %></code></p>

                <div class="card">
                    <h2>Domains</h2>
                    <% if (domains.length === 0) { %>
                        <p>No domains added yet.</p>
                    <% } else { %>
                        <% domains.forEach(domain => { %>
                            <div class="domain-entry">
                                <div class="domain-header">
                                    <h3><%= domain.domainName %></h3>
                                    <% if (domain.verified) { %>
                                        <span class="status-verified">Verified</span>
                                    <% } else { %>
                                        <span class="status-pending">Pending Verification</span>
                                    <% } %>
                                </div>
                                
                                <div class="dns-records">
                                    <h4>DNS Records for Deliverability</h4>
                                    
                                    <div class="record">
                                        <span class="record-type">VERIFY</span>
                                        <p>This record is required to prove you own the domain.</p>
                                        <table>
                                            <tr><th>Type</th><th>Hostname</th><th>Value</th></tr>
                                            <tr>
                                                <td>TXT</td>
                                                <td><%= domain.domainName %></td>
                                                <td><code>my-email-service-verification=<%= domain.domainName %></code></td>
                                            </tr>
                                        </table>
                                        <% if (!domain.verified) { %>
                                            <a href="/admin/clients/<%= client.id %>/domains/<%= domain.id %>/verify" class="button-secondary">Verify Now</a>
                                        <% } %>
                                    </div>

                                    <div class="record">
                                        <span class="record-type">SPF</span>
                                        <p>This record lists authorized IP addresses that can send email for your domain.</p>
                                        <table>
                                            <tr><th>Type</th><th>Hostname</th><th>Value</th></tr>
                                            <tr>
                                                <td>TXT</td>
                                                <td><%= domain.domainName %></td>
                                                <td><code>v=spf1 a mx ip4:79.137.72.109 ~all</code></td>
                                            </tr>
                                        </table>
                                    </div>

                                    <div class="record">
                                        <span class="record-type">DKIM</span>
                                        <p>This record adds a digital signature to your emails to prevent tampering.</p>
                                        <% if (domain.dkimPublicKey) { %>
                                            <table>
                                                <tr><th>Type</th><th>Hostname</th><th>Value</th></tr>
                                                <tr>
                                                    <td>TXT</td>
                                                    <td><%= domain.dkimSelector %>._domainkey.<%= domain.domainName %></td>
                                                    <td><code>v=DKIM1; k=rsa; p=<%= domain.dkimPublicKey %></code></td>
                                                </tr>
                                            </table>
                                        <% } else { %>
                                            <p>DKIM key not generated yet.</p>
                                            <form action="/admin/clients/<%= client.id %>/domains/<%= domain.id %>/generate-dkim" method="POST">
                                                <button type="submit">Generate DKIM Key</button>
                                            </form>
                                        <% } %>
                                    </div>

                                    <div class="record">
                                        <span class="record-type">DMARC</span>
                                        <p>This record tells receiving servers what to do if SPF or DKIM checks fail.</p>
                                        <table>
                                            <tr><th>Type</th><th>Hostname</th><th>Value</th></tr>
                                            <tr>
                                                <td>TXT</td>
                                                <td>_dmarc.<%= domain.domainName %></td>
                                                <td><code>v=DMARC1; p=none;</code></td>
                                            </tr>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        <% }); %>
                    <% } %>
                </div>

                <div class="card">
                    <h2>Add New Domain</h2>
                    <form action="/admin/clients/<%= client.id %>/domains" method="POST">
                        <div class="form-group">
                            <label for="domainName">Domain Name (e.g., my-saas.com)</label>
                            <input type="text" id="domainName" name="domainName" required>
                        </div>
                        <button type="submit">Add Domain</button>
                    </form>
                </div>
            </div>
        </main>
    </div>
</body>
</html>
